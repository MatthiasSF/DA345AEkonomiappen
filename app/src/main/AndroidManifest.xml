<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ai0204.p1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ExistingActivity" />
        <activity android:name=".NewPersonActivity" />
        <activity android:name=".PickedPersonAltActivity" />
        <activity android:name=".IncomeInputActivity" />
        <activity android:name=".ExpenseInputActivity" />
        <activity android:name=".CollectionActivity"></activity>
    </application>

</manifest>